{
  "scripts": {
    "dev": "parcel index.html --open",
    "build": "rm -r dist; parcel build index.html --no-source-maps --public-url='/powerlifting' && ls -l dist",
    "analyze": "elm-analyse --serve --port=1235 --open"
  },
  "dependencies": {
    "elm": "^0.19.0-no-deps",
    "parcel-bundler": "^1.12.3"
  },
  "devDependencies": {
    "elm-hot": "^1.0.1",
    "node-elm-compiler": "^5.0.3"
  }
}
