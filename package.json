{
  "scripts": {
    "dev": "yarn run parcel index.html --open",
    "build": "rm -r dist && yarn run parcel build index.html --no-source-maps && ls -l dist"
  },
  "dependencies": {
    "elm": "^0.19.0-no-deps",
    "parcel-bundler": "^1.12.3"
  },
  "devDependencies": {
    "elm-hot": "^1.0.1",
    "node-elm-compiler": "^5.0.3"
  }
}
